{
  "name": "taskmanagement",
  "version": "1.1.6",
  "private": true,
  "description": "Manage Markdown task notes with frontmatter-powered kanban and timeline views.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs --production",
    "release:check": "node scripts/validate-release.mjs",
    "release:package": "npm run build && node scripts/package-release.mjs",
    "version": "node scripts/version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "latest"
  }
}
